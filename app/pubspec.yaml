name: falcons_esports_controller
description: An app for controlling the overlay written in dart 'cause why not
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.3.1

# Add regular dependencies here.
dependencies:
  git: ^2.2.1
  rw_git: ^1.0.3
  # path: ^1.8.0

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
